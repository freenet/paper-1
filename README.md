# Freenet Whitepaper

A whitepaper describing the architecture of the new Freenet: contract-defined
commutative monoids, summary/delta synchronization, small-world adaptive routing,
and the delegate model.

## Build

```
make
```

Requires `pdflatex` and `bibtex` (TeX Live).
